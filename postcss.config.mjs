// JSDoc type annotation for the PostCSS configuration object.
/** @type {import('postcss-load-config').Config} */
const config = {
  // A map of PostCSS plugins to use.
  plugins: {
    // The Tailwind CSS plugin.
    tailwindcss: {},
  },
};

// Export the PostCSS configuration object.
export default config;
